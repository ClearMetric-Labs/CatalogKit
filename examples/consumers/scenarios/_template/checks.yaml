cases:
  - id: example_node_kinds
    artifact: catalog
    expect_node_kinds:
      - table
      - column
  - id: example_impact_resolves
    impact: orders.amount_upstream
    expect_selection_id: column:orders.amount
